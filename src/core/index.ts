export { ModalStackManager } from './ModalStackManager';
export type { StackEventType, StackEvent, StackChangeListener, StackEventListener } from './ModalStackManager';

export { ModalRegistry } from './ModalRegistry';

export { modalReducer, initialModalState, modalActions } from './modalReducer';

export {
  DEFAULT_BASE_Z_INDEX,
  DEFAULT_Z_INDEX_INCREMENT,
  DEFAULT_ANIMATION_DURATION,
  DEFAULT_BACKDROP_OPACITY,
  DEFAULT_BACKDROP_COLOR,
  WEB_PORTAL_CONTAINER_ID,
  FOCUSABLE_SELECTORS,
} from './constants';
